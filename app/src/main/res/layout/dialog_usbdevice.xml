<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="538.0dip"
    android:layout_height="476.0dip"
    android:background="@drawable/dialog_usbdevice_bg"
    android:orientation="vertical"
    android:paddingBottom="10.0dip"
    android:paddingLeft="6.0dip"
    android:paddingRight="6.0dip" >

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="60.0dip" >

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="请选择需要连接的打印机"
            android:textColor="@android:color/white"
            android:textSize="20.0sp" />
    </RelativeLayout>

    <ListView
        android:id="@+id/lv_dialog_usbdevice"
        android:layout_width="fill_parent"
        android:layout_height="332.0dip"
        android:background="@android:color/transparent"
        android:cacheColorHint="@android:color/transparent"
        android:divider="@null"
        android:dividerHeight="6.0dip"
        android:fadingEdge="none"
        android:listSelector="@android:color/transparent"
        android:paddingBottom="6.0dip"
        android:scrollbars="none"
        android:scrollingCache="true" />

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="76.0dip" >

        <Button
            android:id="@+id/btn_usbdiver_serch"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:background="@drawable/selector_btn_dialog_usbdiver"
            android:paddingBottom="14.0dip"
            android:text="重新搜索"
            android:textColor="@android:color/white"
            android:textSize="20.0sp" />
    </RelativeLayout>

</LinearLayout>